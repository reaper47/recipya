// Package main provides a tool for building and releasing a Go application. The tool takes the package name and
// the release tag as input and builds the package for all supported platforms. The built binaries are then
// zipped and saved in the releases/<tag> directory.
package main
